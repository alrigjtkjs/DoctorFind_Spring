<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fd.md.mapper.HealthinfoMapper">
	<select id="list" resultType="Healthinfo">
		select * from HEALTHINFO where MONTH=#{month} order by ID desc
	</select>
</mapper>